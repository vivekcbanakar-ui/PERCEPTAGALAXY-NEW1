import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Percepta Galaxy - Real-time Competitive Intelligence",
  description: "Track your competitors' every move. AI-powered insights. Same-day alerts.",
  keywords: "competitive intelligence, market research, competitor tracking, SaaS",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
